Feature contract

- `template.html` - DOM structure and ids
- `styles.css` - optional feature stylesheet
- `index.js` or `render(container)` - exports mounting function

Data: Fetch from `/api/analytics` for metrics and sample data.
